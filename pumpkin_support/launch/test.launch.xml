<?xml version="1.0"?>
<launch>
    <include file="$(find-pkg-share pumpkin_support)/launch/load.launch.xml" />

    <!-- Input Arguments -->
    <arg name="sim_robot" default="True" description="[Bool] Robot Joint State Simulated or listen for /joint_states topic?" />
    <arg name="rviz_config" default="$(find-pkg-share pumpkin_support)/config/visualize_description.rviz" description="[String] Path to RViz config file." />

    <!-- Simulate Joint State -->
    <group if="$(var sim_robot)" >
        <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui" output="screen" />
    </group>

    <!-- Real Robot Joint States -->
    <group unless="$(var sim_robot)" >
        <node pkg="joint_state_publisher" exec="joint_state_publisher">
            <param name="source_list" value="[ctrl_groups/r1/joint_states]"/>
        </node>
    </group>

    <!-- Publish the robot state (tf transforms) -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher" output="screen" >
        <param name="robot_description" value="$(var robot_description)" />
    </node>

    <!-- User Interaction -->
    <node pkg="rviz2" exec="rviz2" args="-d $(var rviz_config)" >
        <param name="robot_description" value="$(var robot_description)" />
    </node>

</launch>